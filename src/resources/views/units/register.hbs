<div class="auth-form col-md-3">

    <div class="auth-form__header row">
        <h3 class="auth-form__heading col"><a href="/login">SIGN IN</a></h3>
        <h3 class="auth-form__switch-btn col">SIGN UP</h3>
    </div>

    <div class="auth-form__form">

        <form action="#" class="mt-4 mb-4" method="">
            <div class="form-group">
                <input type="text" class="form-control" id="unit-code" name='unit-code' value="A01">
            </div>
            <div class="form-group">
                <input type="password" class="form-control" id="password" name='password' placeholder="Password" required="true">
            </div>
            <div class="form-group">
                <input type="password" class="form-control" id="password-repeat" placeholder="Password" required="true">
                </label>
            </div>
            <button type="submit" class="btn btn-primary" onclick="reg()">Sign up</button>
        </form>
    </div>

</div>

<script>
    function reg() {
        var password = document.getElementById('password').value;
        var password_repeat = document.getElementById('password-repeat').value;

        if (password != password_repeat) {
            alert('Mật khẩu không trùng nhau mời nhập lại');
            return;
        }

        /*fetch('/dangky',{
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify({"id": id, 
                                  "password": psw})
            }).then(function (response) {
                console.log(response);
                document.location.pathname = "/"
            })*/
    }
</script>